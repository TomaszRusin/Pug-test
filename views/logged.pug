
<!DOCTYPE html>
html(lang="en")
    head
        title Simple template
    body
        if(user)
            h1 Hi, #{user.first_name} #{user.last_name}
        else 
            a(href="/sign_up") Sign up